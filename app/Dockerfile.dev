FROM node:18-alpine

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy source code including public directory
COPY . .

EXPOSE 3000

# Use start command for development (with hot reload)
# Source code will be mounted as volume for hot reloading
CMD ["npm", "start"]
